<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="stay.with.me.api.model.mapper.HouseReviewMapper">
    <select id="get" resultType="stay.with.me.api.model.dto.HouseReviewDto">
        SELECT r.review_id,
               r.house_detail_id,
               d.house_description as house_detail_desc,
               r.star,
               r.review_detail,
               r.created_by,
               r.created_at
          FROM house_review r
    INNER JOIN house_detail d
            ON r.house_detail_id = d.house_detail.id
         WHERE 1 = 1
           AND r.review_id = #{reviewId}
    </select>

    <select id="getList" resultType="stay.with.me.api.model.dto.HouseReviewDto">
        SELECT r.review_id,
               r.house_detail_id,
               d.house_description as house_detail_desc,
               r.star,
               r.review_detail,
               r.created_by,
               r.created_at
          FROM house_review r
    INNER JOIN house_detail d
            ON r.house_detail_id = d.house_detail_id
         WHERE 1 = 1
        AND r.house_detail_id IN
        <foreach collection="houseDetailIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="create" parameterType="stay.with.me.api.model.dto.HouseReviewDto">
        INSERT INTO house_review
        (
        house_detail_id,
        star,
        review_detail,
        created_by,
        created_at
        )
        values
        (
        #{houseDetailId},
        #{star},
        #{reviewDetail},
        #{createdBy},
        CURRENT_TIMESTAMP
        )
    </insert>

    <update id="update" useGeneratedKeys="true" keyProperty="reviewId">
        UPDATE house_review
        <set>
            <if test="star != null">
                star = #{star},
            </if>
            <if test="reviewDetail != null">
                review_detail = #{reviewDetail},
            </if>
        </set>
        WHERE review_id = #{reviewId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM house_review
        WHERE review_id = #{reviewId}
    </delete>
</mapper>