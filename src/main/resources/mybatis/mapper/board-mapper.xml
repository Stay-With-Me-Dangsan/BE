<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="stay.with.me.api.model.mapper.BoardMapper">

    <select id="findAll" resultType="stay.with.me.api.model.dto.BoardDTO">
        SELECT * FROM board
    </select>

    <select id="findById" parameterType="long" resultType="stay.with.me.api.model.dto.BoardDTO">
        SELECT * FROM board WHERE post_id = #{id}
    </select>

    <insert id="insert" parameterType="stay.with.me.api.model.dto.BoardDTO">
        INSERT INTO board (user_id, board_type, title, content, created_at, updated_at, views, status, type)
        VALUES (#{userId}, #{boardType}, #{title}, #{content}, NOW(), NOW(), #{views}, #{status}, #{type})
    </insert>

    <update id="update" parameterType="stay.with.me.api.model.dto.BoardDTO">
        UPDATE board
        SET title = #{title}, content = #{content}, updated_at = NOW()
        WHERE post_id = #{postId}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM board WHERE post_id = #{id}
    </delete>

</mapper>
